import React from 'react';

const Aboutlayout = ({children}) => {
    return (
        <div >
            <div className='bg-green-700'>About Layout</div>
            {children}
        </div>
    );
};

export default Aboutlayout;